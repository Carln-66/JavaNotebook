/*
 *          一、理解万事万物皆对象
 *          1. 在Java语言中，我们都将功能、结构封装到类中，通过类的实例化，来调用具体的功能结构
 *              > Scanner, String等
 *              > 文件：File
 *              > 网络资源：URL
 *          2. 涉及到Java语言与前端HTML、后端的数据库交互时，前后端的结构在Java层面交互时，都体现为类、对象
 *
 *          二、内存解析说明
 *          1. 引用类型的变量，只可能存储两类值：null或地址值(含变量的类型)
 */